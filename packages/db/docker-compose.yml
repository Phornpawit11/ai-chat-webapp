name: ai-chat-webapp

services:
  postgres:
    image: postgres
    container_name: ai-chat-webapp-postgres
    environment:
      POSTGRES_DB: ai-chat-webapp
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - ai-chat-webapp_postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  ai-chat-webapp_postgres_data:
